import styled from 'styled-components';

export const StyledOrderModalWrapper = styled.div`
  position: absolute;
  bottom: 0;
  width: 100%;
  height: 100vh;
`;

export const StyledOrderModalText = styled.p`
  text-align: center;
`;
