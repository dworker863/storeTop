import { FC } from 'react';
import { StyledModal } from '../../../commonStyles/StyledModal';

const OrderModal: FC = () => {
  return <StyledModal active={false}>OrderModal</StyledModal>;
};

export default OrderModal;
